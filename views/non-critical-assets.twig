<script>
	var path = '{{ template_directory_uri }}';

	function loadCSS( href, before, media ){
		"use strict";
		var ss = window.document.createElement( "link" );
		var ref = before || window.document.getElementsByTagName( "script" )[ 0 ];
		ss.rel = "stylesheet";
		ss.href = href;
		ss.media = "only x";
		ref.parentNode.insertBefore( ss, ref );
		setTimeout( function(){
			ss.media = media || "all";
		} );
		return ss;
	 }
	loadCSS( path + "/dest/css/non-critical.css" );

	function loadJS( src ){
		"use strict";
		var ref = window.document.getElementsByTagName( "script" )[ 0 ];
		var script = window.document.createElement( "script" );
		script.src = src;
		ref.parentNode.insertBefore( script, ref );
		return script;
	}
	loadJS( path + "/dest/js/bundle.js" );
</script>
<noscript>
	<link rel="stylesheet" href="{{ template_directory_uri }}/dest/css/non-critical.css">
</noscript>